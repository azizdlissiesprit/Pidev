<form method="post" action="{{ path('app_reponsequiz_delete', {'id_rep': reponsequiz.id_rep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponsequiz.id_rep) }}">
    <button class="btn">Delete</button>
</form>
