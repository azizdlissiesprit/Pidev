{% extends 'base.html.twig' %}

{% block title %}WebinarResources{% endblock %}

{% block body %}
    <h1>WebinarResources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ webinar_resource.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ webinar_resource.title }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ webinar_resource.type }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ webinar_resource.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_webinar_resources_index') }}">back to list</a>

    <a href="{{ path('app_webinar_resources_edit', {'id': webinar_resource.id}) }}">edit</a>

    {{ include('webinar_resources/_delete_form.html.twig') }}
{% endblock %}
