{% extends 'base.html.twig' %}

{% block title %}WebinarFeedback{% endblock %}

{% block body %}
    <h1>WebinarFeedback</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ webinar_feedback.id }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ webinar_feedback.rating }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ webinar_feedback.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_webinar_feedback_index') }}">back to list</a>

    <a href="{{ path('app_webinar_feedback_edit', {'id': webinar_feedback.id}) }}">edit</a>

    {{ include('webinar_feedback/_delete_form.html.twig') }}
{% endblock %}
