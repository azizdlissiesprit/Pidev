<form method="post" action="{{ path('app_webinar_chat_delete', {'id': webinar_chat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ webinar_chat.id) }}">
    <button class="btn">Delete</button>
</form>
